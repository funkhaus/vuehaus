* When you define a state, you'll want to define a query
* Query files run a query, then run that query's results through a serializer and output as $data


TODO: Handle instant state changing/URL lag
